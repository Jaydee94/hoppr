{
  "name": "hoppr-release-tooling",
  "private": true,
  "description": "Dev-only Node tooling for hoppr — commit message linting.",
  "scripts": {
    "commitlint": "commitlint --from=HEAD~1"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.0.1",
    "@commitlint/config-conventional": "^21.0.2"
  }
}
